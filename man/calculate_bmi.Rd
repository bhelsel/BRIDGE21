% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{calculate_bmi}
\alias{calculate_bmi}
\title{Calculate Body Mass Index (BMI)}
\usage{
calculate_bmi(
  data,
  weight,
  height,
  units = c("metric", "imperial"),
  digits = 1
)
}
\arguments{
\item{data}{A data frame containing weight and height columns}

\item{weight}{Column name for weight (unquoted). Should be in kg for metric
or pounds for imperial units}

\item{height}{Column name for height (unquoted). Should be in cm for metric
or inches for imperial units}

\item{units}{Character string specifying the unit system. Either "metric"
(default) or "imperial"}

\item{digits}{The number of decimal points for which to round the BMI value}
}
\value{
The input data frame with an additional \code{bmi} column containing
calculated BMI values
}
\description{
Calculates BMI from weight and height columns in a data frame. Handles both
metric and imperial units with automatic conversion to metric for calculation.
}
\details{
BMI is calculated using the formula: weight (kg) / (height (m))^2

For imperial units, the function converts:
\itemize{
\item Weight: pounds to kg (divide by 2.205)
\item Height: inches to cm (multiply by 2.54)
}
}
\examples{
# Metric units (kg and cm)
df_metric <- data.frame(weight_kg = c(70, 80), height_cm = c(170, 180))
calculate_bmi(df_metric, weight_kg, height_cm, units = "metric")

# Imperial units (pounds and inches)
df_imperial <- data.frame(weight_lbs = c(154, 176), height_in = c(67, 71))
calculate_bmi(df_imperial, weight_lbs, height_in, units = "imperial")

}
