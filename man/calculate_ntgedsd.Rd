% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_ntgedsd.R
\name{calculate_ntgedsd}
\alias{calculate_ntgedsd}
\title{Calculate NTG-EDSD Domain Scores}
\usage{
calculate_ntgedsd(data, domain, label)
}
\arguments{
\item{data}{A data frame containing NTG-EDSD item responses. Column names
should include substrings corresponding to each NTG-EDSD domain (e.g.,
\code{"adl"}, \code{"lang"}, \code{"sleep"}, \code{"mem"}).}

\item{domain}{A short domain identifier (e.g., \code{"adl"}, \code{"lang"}, \code{"mem"}).
When \code{"all"}, the function uses all columns to calculate a total score.}

\item{label}{A descriptive name for the output column (e.g.,
\code{"Activities of Daily Living"}). Typically supplied automatically when used
within a mapping call such as \code{purrr::imap_dfc()}.}
}
\value{
A one-column tibble containing the summed score for the specified domain, with
the column name set to the provided \code{label}.
}
\description{
Computes domain-level or total NTG-EDSD scores from a dataset by summing items
coded as 1 or 2, indicating the presence of a change. The function returns a
tibble containing a single column named after the provided domain label.
}
\details{
Each domain score is calculated as the number of items coded as \code{1} or \code{2}
within the corresponding set of columns. Missing values are ignored when
summing.

This function is typically used inside an iterative call such as
\code{purrr::imap_dfc()} to compute all NTG-EDSD domain scores at once.
}
\examples{
\dontrun{
ntgedsd_domains <- c(
  all = "Total Score",
  adl = "Activities of Daily Living",
  mem = "Memory"
)

purrr::imap_dfc(ntgedsd_domains, function(label, domain) {
  calculate_ntgedsd(ntgedsd, domain, label)
})
}

}
