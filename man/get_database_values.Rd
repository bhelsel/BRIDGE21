% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_database_values}
\alias{get_database_values}
\title{Retrieve Non-Missing Values from a Dataset}
\usage{
get_database_values(..., data, event_name = NULL)
}
\arguments{
\item{...}{One or more unquoted column names to extract.}

\item{data}{A data frame or tibble containing the variables of interest.}

\item{event_name}{(Optional) A variable name representing an event column, used to subset data along with the specified variables.}
}
\value{
A data frame containing the specified columns with all rows containing \code{NA} values removed.
}
\description{
Extracts specified columns (and optionally an event column) from a dataset and drops any rows containing missing values.
}
\details{
This function is designed to simplify the process of subsetting and cleaning datasets, particularly for event-based data
(e.g., longitudinal REDCap exports). If \code{event_name} is supplied, the function ensures the event column is included and
removes rows with missing data across all selected variables. If not, only the specified variables are included.
}
\examples{
\dontrun{
get_database_values(age, sex, data = mydata)
get_database_values(age, bmi, data = mydata, event_name = redcap_event_name)
}

}
