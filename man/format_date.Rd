% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{format_date}
\alias{format_date}
\title{Format Date Columns}
\usage{
format_date(data, date, desc = FALSE, display = "\%m/\%d/\%Y")
}
\arguments{
\item{data}{A data frame containing date information}

\item{date}{Date specification (unquoted). Can be:
\itemize{
\item A single column name (e.g., \code{visitdt})
\item Composite date using \code{/} operator (e.g., \code{visitmo/visitday/visityr})
\item A \code{paste()} expression (e.g., \code{paste(visitmo, visitday, visityr, sep = "/")})
}}

\item{desc}{Display the date in descending order}

\item{display}{Customize how the dates are displayed, Default: \%m/\%d/\%Y}
}
\value{
The input data frame with a "Date" column formatted as "mm/dd/yyyy".
For single column input, the original column is renamed to "Date". For
composite dates, the "Date" column is added while preserving original columns.
}
\description{
Formats date information into a standardized "Date" column with mm/dd/yyyy
format. Handles single date columns or composite dates built from separate
month, day, and year columns using tidy evaluation.
}
\details{
The function uses rlang metaprogramming to accept unquoted column names.

For composite dates using the \code{/} operator, the function:
\enumerate{
\item Intercepts the expression before numeric division occurs
\item Converts it to a paste operation to create date strings
\item Parses the strings as dates using the "\%m/\%d/\%Y" format
}

For single date columns, the function attempts to parse using \code{as.Date()}
which handles common date formats automatically.
}
\examples{
# Single date column
df1 <- data.frame(visitdt = c("2024-01-15", "2024-02-20"))
format_date(df1, visitdt)

# Composite date from separate columns using / operator
df2 <- data.frame(
  visitmo = c(1, 2),
  visitday = c(15, 20),
  visityr = c(2024, 2024)
)
format_date(df2, visitmo/visitday/visityr)

# Composite date using paste()
format_date(df2, paste(visitmo, visitday, visityr, sep = "/"))

}
